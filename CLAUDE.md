# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

A GitHub profile repo (repo name matches the `thediversecandidate`
account/org name, so its README renders on the GitHub profile page) that
indexes the other `thediversecandidate` projects — currently listing
GenesisAI, Realtime-voice-feedback-AI, EmailAnalysis, and itself as a "meta
repository coordinating all sub-projects."

## Working here

`README.md` is the only real content — the project-index table. (It used to
live in `README_backup.md` instead, which meant it wasn't actually
rendering on the profile page since GitHub only renders a file literally
named `README.md`; that's been fixed — `README_backup.md` and a stray,
unrelated `index.html` from the `webscraper-React-FrontEnd` repo were both
removed.) When a listed sub-project changes (renamed, archived, new project
added), update the table in `README.md` directly.
